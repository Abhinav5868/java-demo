version: '3'
services:
  web:
    image: "${abhinav5868}/demo-app:${imageTag}"  # Correctly quote and substitute environment variables
    ports:
      - "8081:8080"  # Ensure proper spacing and no invalid characters
